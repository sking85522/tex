linux-x86_64:
  exclude:
    - lib/linux-arm64
    - lib/darwin-*
    - lib/windows-*
    - "*.exe"
    - "*.dylib"
    - "**/*.windows.*"

linux-arm64:
  exclude:
    - lib/linux-x86_64
    - lib/windows-*
    - lib/darwin-*
    - "*.exe"
    - "*.dylib"
    - "**/*.windows.*"

darwin-x86_64:
  exclude:
    - lib/darwin-arm64
    - lib/windows-*
    - lib/linux-*
    - "*.exe"
    - "*.so"
    - "**/*.linux.*"

darwin-arm64:
  exclude:
    - lib/darwin-x86_64
    - lib/windows-*
    - lib/linux-*
    - "*.exe"
    - "*.so"
    - "**/*.linux.*"

windows-x86_64:
  exclude:
    - lib/linux-*
    - lib/darwin-*
    - "*.so"
    - "*.dylib"
    - "**/*.darwin.*"
    - "**/*.linux.*"
