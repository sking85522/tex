<?php

namespace NumPHP\Utils;

use NumPHP\Core\NDArray;

class UniqueCounts
{
    public static function unique_counts(...$args)
    {
        return \NumPHP\NumPHP::unique_counts(...$args);
    }
}
