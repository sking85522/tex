<?php

namespace NumPHP\ArrayManipulation;

use NumPHP\Core\NDArray;

class ApplyAlongAxis
{
    public static function apply_along_axis(...$args)
    {
        return \NumPHP\NumPHP::apply_along_axis(...$args);
    }
}
